import React, { FC } from 'react'
import { Routes, Route } from 'react-router-dom';
import Home from '../../Home';
interface ModerationProps {

}

const Moderation: FC<ModerationProps> = ({ }) => {
      return (
            <div>
                  Moder
            </div>
      )
}

export default Moderation;