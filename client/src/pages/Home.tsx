import React, { FC } from 'react'


interface HomeProps {
      children?: JSX.Element | string;
}

const Home: FC = (props: HomeProps) => {

      return (
            <>
                  Home
            </>
      )
}

export default Home;
